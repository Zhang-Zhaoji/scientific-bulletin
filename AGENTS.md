# 神经科学快讯封面工作流

本文件适用于仓库根目录及所有子目录。处理周报封面时遵循以下约定。

## 确认缺失期号

1. 在 `LLM_Results/` 中按日期检查常规周报，排除文件名含 `wechat`、`specialissue`、`.raw` 或 `.deprecated` 的文件。
2. 将常规周报日期与 `Imgs/YYYYMMDD.png` 对照，只为缺少正式日期封面的周报制作新图。
3. 期号只沿常规周报递增，专题期不占常规期号。不要通过全部 Markdown 文件数量推算期号。
4. 已确认的连续基准为：2026-07-18 是第 019 期；2026-07-26 至 2026-09-13 是第 020 至 027 期。

## 选择每期主题

1. 阅读对应 Markdown 开头的本周概览、头条推荐和精选文献。
2. 优先采用头条推荐；没有头条时，从最靠前、分数高且视觉表达清晰的文章中选择一个主题。
3. 将主题浓缩为两行中文：第一行是研究对象或方法，第二行是主要发现。不要在封面上堆叠摘要全文。

## 生成底图

1. 参考 `Imgs/` 中最近几期的正式封面，特别是 `20260711.png` 和 `20260718.png`。
2. 每一期单独生成一张底图。统一要求：
   - 1448×1086 或可无损调整到该尺寸的 4:3 横图；
   - 结构主义、构成主义政治宣传画、低维几何体/低维多面体、版画质感；
   - 主色只使用红、金、黑和暖象牙白；
   - 科学主题主体放在中部或右侧，允许最终标题板覆盖一部分画面；
   - 底图不生成任何文字、数字、标志或伪文字。
3. 底图属于中间产物，放在临时目录，不提交到 Git。

## 叠加正式文字

1. 在 `scripts/compose_bulletin_covers.py` 的 `COVERS` 中更新日期、期号和两行主题。
2. 使用 `panel-large` 样式生成正式封面。例如：

   ```powershell
   python -B scripts\compose_bulletin_covers.py --source-dir <底图目录> --style panel-large
   ```

3. 正式版式使用微软雅黑：
   - 刊名“神经科学快讯”使用黑色“神经科学”与红色“快讯”；
   - 米白色构成主义标题板配红色期号块和金色斜边；
   - 标题、期号、日期和主题均采用当前 `panel-large` 的大字号；
   - 日期格式为 `YYYY/MM/DD`，期号格式为三位数，例如“第 028 期”。
4. 正式文件只能命名为 `Imgs/YYYYMMDD.png`。不要提交 `-v2`、`-v3`、`-v4`、背景图或审阅联系表。

## 检查与提交

1. 确认每张正式封面均为 1448×1086，能够正常打开，刊名、期号、日期和中文主题没有错字或截断。
2. 批量制作时可以临时生成联系表进行目视检查；确认后删除联系表。
3. 工作区可能包含用户的其他未提交结果。只暂存本次明确要求的封面、脚本和文档，不要顺带提交其他文件。
4. 未经用户明确要求，不要推送远端。用户要求推送时，先检查暂存区，再提交并推送当前分支。
